<!DOCTYPE html>
<html>
  <head>
    <title>MAGI QA - Test Management</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="font-mono <%= light_theme? ? 'theme-light' : 'theme-dark' %>">
    <% if user_signed_in? %>
      <!-- Sidebar -->
      <%= render 'shared/sidebar' %>

      <!-- Main Content Area -->
      <div class="ml-64 min-h-screen">
        <!-- Header Bar -->
        <header class="header-dark">
          <div class="flex items-center justify-between px-6 py-3">
            <div class="flex items-center space-x-6">
              <!-- Page Title -->
              <div class="flex items-center space-x-3">
                <span class="text-[10px] font-mono text-theme-muted uppercase tracking-widest">Page:</span>
                <h1 class="text-sm font-mono uppercase tracking-wider text-theme-primary">
                  <%= content_for?(:page_title) ? yield(:page_title) : 'Dashboard' %>
                </h1>
              </div>

              <% if content_for?(:breadcrumbs) %>
                <div class="divider-vertical"></div>
                <nav class="flex items-center text-xs font-mono text-theme-secondary">
                  <%= yield(:breadcrumbs) %>
                </nav>
              <% end %>
            </div>

            <div class="flex items-center space-x-4">
              <!-- System Status -->
              <div class="system-status">
                <span class="system-status-dot"></span>
                <span class="system-status-text">System Online</span>
              </div>

              <!-- Theme Toggle -->
              <div data-controller="theme-toggle">
                <button data-action="click->theme-toggle#toggle"
                        class="flex items-center space-x-2 px-3 py-1.5 border border-theme rounded text-xs font-mono text-theme-secondary hover:text-theme-primary hover:border-primary transition-colors"
                        title="Toggle Theme">
                  <svg data-theme-toggle-target="icon" class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"/>
                  </svg>
                  <span data-theme-toggle-target="label" class="hidden sm:inline">Light</span>
                </button>
              </div>

              <!-- Search -->
              <%= form_with url: search_path, method: :get, local: true, class: "relative" do %>
                <input type="text" name="q" placeholder="Search..." class="input-dark w-48 pl-8 pr-4 py-1.5 text-xs">
                <svg class="w-4 h-4 text-theme-muted absolute left-2.5 top-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
                </svg>
              <% end %>

              <!-- Notifications -->
              <div data-controller="notifications"
                   data-notifications-user-id-value="<%= current_user.id %>"
                   data-action="click@window->notifications#close"
                   class="relative">
                <button data-action="click->notifications#toggle"
                        class="relative p-2 text-theme-muted hover:text-status-warning transition-colors">
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"/>
                  </svg>
                  <span data-notifications-target="badge"
                        class="absolute -top-1 -right-1 px-1.5 py-0.5 bg-status-error text-white text-[9px] font-mono rounded-full <%= current_user.unread_notifications_count > 0 ? '' : 'hidden' %>">
                    <%= current_user.unread_notifications_count > 99 ? '99+' : current_user.unread_notifications_count %>
                  </span>
                </button>

                <!-- Dropdown -->
                <div data-notifications-target="dropdown"
                     class="hidden absolute right-0 mt-2 w-80 bg-dark-surface border border-dark-border rounded shadow-lg z-50">
                  <div class="flex items-center justify-between px-4 py-2 border-b border-dark-border">
                    <span class="text-[10px] font-mono text-text-secondary uppercase tracking-wider">Notifications</span>
                    <%= link_to notifications_path, class: "text-[10px] font-mono text-primary uppercase tracking-wider hover:text-primary-light" do %>
                      View All
                    <% end %>
                  </div>

                  <div data-notifications-target="emptyState" class="hidden p-4 text-center">
                    <p class="text-text-muted font-mono text-xs uppercase">No notifications</p>
                  </div>

                  <div data-notifications-target="list" class="max-h-80 overflow-y-auto">
                    <!-- Notifications will be loaded dynamically -->
                  </div>
                </div>
              </div>

              <!-- User Avatar -->
              <div class="avatar avatar-sm avatar-dark">
                <%= current_user.display_name.first.upcase %>
              </div>
            </div>
          </div>
        </header>

        <!-- Main Content Area -->
        <main class="p-6">
          <!-- System Alerts -->
          <% if notice %>
            <div class="alert alert-success mb-6" role="alert">
              <div class="flex items-center space-x-3">
                <span class="w-2 h-2 bg-status-success rounded-full"></span>
                <span class="uppercase tracking-wider text-xs">System Notice:</span>
                <span><%= notice %></span>
              </div>
              <button onclick="this.parentElement.remove()" class="hover:opacity-70">
                <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"/>
                </svg>
              </button>
            </div>
          <% end %>

          <% if alert %>
            <div class="alert alert-error mb-6" role="alert">
              <div class="flex items-center space-x-3">
                <span class="w-2 h-2 bg-status-error rounded-full animate-pulse"></span>
                <span class="uppercase tracking-wider text-xs">Critical Alert:</span>
                <span><%= alert %></span>
              </div>
              <button onclick="this.parentElement.remove()" class="hover:opacity-70">
                <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"/>
                </svg>
              </button>
            </div>
          <% end %>

          <%= yield %>
        </main>

        <!-- Footer -->
        <footer class="footer-dark">
          <div class="flex items-center justify-between footer-text">
            <span>MAGI QA v1.0</span>
            <span data-controller="system-clock" data-system-clock-target="display"><%= Time.now.strftime('%Y-%m-%d %H:%M:%S') %></span>
          </div>
        </footer>
      </div>
    <% else %>
      <!-- Authentication Interface -->
      <div class="min-h-screen flex items-center justify-center bg-dark-base">
        <div class="max-w-md w-full mx-4">
          <div class="text-center mb-8">
            <!-- Logo -->
            <div class="w-24 h-24 mx-auto mb-6 flex items-center justify-center bg-primary-muted rounded-lg border border-primary">
              <span class="text-4xl font-mono font-bold text-primary-light">MQ</span>
            </div>
            <h1 class="text-3xl font-mono font-bold text-primary-light tracking-widest">MAGI QA</h1>
            <p class="text-text-muted font-mono text-sm tracking-wider mt-2">Test Management System</p>
            <p class="text-primary/50 font-mono text-[10px] tracking-wider mt-4">SIGN IN TO CONTINUE</p>
          </div>

          <div class="panel p-8">
            <% if notice %>
              <div class="alert alert-success mb-6" role="alert">
                <%= notice %>
              </div>
            <% end %>
            <% if alert %>
              <div class="alert alert-error mb-6" role="alert">
                <%= alert %>
              </div>
            <% end %>

            <%= yield %>
          </div>

          <div class="text-center mt-8">
            <p class="text-[9px] font-mono text-text-muted/40 tracking-wide">
              Quality Assurance Made Simple
            </p>
          </div>
        </div>
      </div>
    <% end %>
  </body>
</html>
