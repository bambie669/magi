<!DOCTYPE html>
<html>
  <head>
    <title>Magi</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <nav class="bg-blue-600 text-white p-4 shadow-md">
      <div class="container mx-auto flex justify-between items-center">
        <%= link_to 'Magi', authenticated_root_path, class: "px-3 py-2 m-2 text-xl font-bold text-red" %>
        <div class="flex items-center">
          <% if user_signed_in? %>
            <%= link_to 'Dashboard', dashboard_path, class: "px-3 py-2 hover:bg-blue-700 rounded" %>
            <%= link_to 'Projects', projects_path, class: "px-3 py-2 hover:bg-blue-700 rounded" %>
            <span class="mx-2">|</span>
            <span class="mr-2">Salut, <%= current_user.display_name %> (<%= current_user.role %>)</span>
            <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: "px-3 py-1 bg-red-500 hover:bg-red-600 rounded text-sm" %>
          <% else %>
            <%= link_to 'Login', new_user_session_path, class: "px-3 py-2 hover:bg-blue-700 rounded" %>
            <%= link_to 'Sign Up', new_user_registration_path, class: "px-3 py-2 hover:bg-blue-700 rounded" %>
          <% end %>
        </div>
      </div>
    </nav>

    <main class="container mx-auto mt-8 px-4">
      <%# Flash Messages %>
      <% if notice %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
          <span class="block sm:inline"><%= notice %></span>
        </div>
      <% end %>
      <% if alert %>
         <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
           <span class="block sm:inline"><%= alert %></span>
         </div>
      <% end %>

      <%= yield %>
    </main>

    <footer class="text-center text-gray-500 mt-10 pb-5">
      Magi - <%= Time.now.year %>
    </footer>
  </body>
</html>