<!DOCTYPE html>
<html>
  <head>
    <title>MAGI QA - Test Management</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="font-mono <%= light_theme? ? 'theme-light' : 'theme-dark' %>">
    <% if user_signed_in? %>
      <!-- Sidebar -->
      <%= render 'shared/sidebar' %>

      <!-- Main Content Area -->
      <div class="ml-64 min-h-screen">
        <!-- Header Bar -->
        <header class="header-dark">
          <div class="flex items-center justify-between px-6 py-3">
            <div class="flex items-center space-x-6">
              <!-- Page Title -->
              <div class="flex items-center space-x-3">
                <span class="text-[10px] font-mono text-theme-muted uppercase tracking-widest">Page:</span>
                <h1 class="text-sm font-mono uppercase tracking-wider text-theme-primary">
                  <%= content_for?(:page_title) ? yield(:page_title) : 'Dashboard' %>
                </h1>
              </div>

              <% if content_for?(:breadcrumbs) %>
                <div class="divider-vertical"></div>
                <nav class="flex items-center text-xs font-mono text-theme-secondary">
                  <%= yield(:breadcrumbs) %>
                </nav>
              <% end %>
            </div>

            <div class="flex items-center space-x-4">
              <!-- System Status -->
              <div class="system-status">
                <span class="system-status-dot"></span>
                <span class="system-status-text">System Online</span>
              </div>

              <!-- Theme Toggle -->
              <div data-controller="theme-toggle">
                <button data-action="click->theme-toggle#toggle"
                        class="flex items-center space-x-2 px-3 py-1.5 border border-theme rounded text-xs font-mono text-theme-secondary hover:text-theme-primary hover:border-primary transition-colors"
                        title="Toggle Theme">
                  <svg data-theme-toggle-target="icon" class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"/>
                  </svg>
                  <span data-theme-toggle-target="label" class="hidden sm:inline">Light</span>
                </button>
              </div>

              <!-- Search -->
              <%= form_with url: search_path, method: :get, local: true do %>
                <input type="text" name="q" placeholder="Search..." class="input-dark w-48 px-4 py-1.5 text-xs">
              <% end %>

              <!-- User Avatar -->
              <div class="avatar avatar-sm avatar-dark">
                <%= current_user.display_name.first.upcase %>
              </div>
            </div>
          </div>
        </header>

        <!-- Main Content Area -->
        <main class="p-6">
          <!-- System Alerts -->
          <% if notice %>
            <div class="alert alert-success mb-6" role="alert">
              <div class="flex items-center space-x-3">
                <span class="w-2 h-2 bg-status-success rounded-full"></span>
                <span class="uppercase tracking-wider text-xs">System Notice:</span>
                <span><%= notice %></span>
              </div>
              <button onclick="this.parentElement.remove()" class="hover:opacity-70">
                <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"/>
                </svg>
              </button>
            </div>
          <% end %>

          <% if alert %>
            <div class="alert alert-error mb-6" role="alert">
              <div class="flex items-center space-x-3">
                <span class="w-2 h-2 bg-status-error rounded-full animate-pulse"></span>
                <span class="uppercase tracking-wider text-xs">Critical Alert:</span>
                <span><%= alert %></span>
              </div>
              <button onclick="this.parentElement.remove()" class="hover:opacity-70">
                <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"/>
                </svg>
              </button>
            </div>
          <% end %>

          <%= yield %>
        </main>

        <!-- Footer -->
        <footer class="footer-dark">
          <div class="flex items-center justify-between footer-text">
            <span>MAGI QA v1.0</span>
            <span data-controller="system-clock" data-system-clock-target="display"><%= Time.now.strftime('%Y-%m-%d %H:%M:%S') %></span>
          </div>
        </footer>
      </div>
    <% else %>
      <!-- Authentication Interface -->
      <div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-slate-950 via-slate-900 to-red-950">
        <!-- Background Pattern -->
        <div class="absolute inset-0 opacity-5">
          <div class="absolute inset-0" style="background-image: radial-gradient(circle at 2px 2px, rgba(255,100,100,0.3) 1px, transparent 0); background-size: 40px 40px;"></div>
        </div>

        <div class="relative max-w-md w-full mx-4">
          <!-- Logo Section -->
          <div class="text-center mb-8">
            <div class="relative inline-block">
              <!-- Glow Effect -->
              <div class="absolute inset-0 blur-2xl opacity-30 bg-red-500 rounded-full scale-150"></div>
              <!-- Logo Image -->
              <%= image_tag 'magi-logo.png', alt: 'MAGI QA', class: 'relative w-32 h-32 mx-auto drop-shadow-[0_0_25px_rgba(239,68,68,0.5)]' %>
            </div>
            <h1 class="text-4xl font-mono font-bold text-transparent bg-clip-text bg-gradient-to-r from-red-400 via-orange-400 to-red-500 tracking-widest mt-4">MAGI QA</h1>
            <p class="text-slate-400 font-mono text-sm tracking-wider mt-2">Test Management System</p>
            <div class="flex items-center justify-center space-x-2 mt-4">
              <span class="w-8 h-px bg-gradient-to-r from-transparent to-red-500/50"></span>
              <span class="text-red-500/70 font-mono text-[10px] tracking-[0.2em] uppercase">Sign In To Continue</span>
              <span class="w-8 h-px bg-gradient-to-l from-transparent to-red-500/50"></span>
            </div>
          </div>

          <!-- Login Card -->
          <div class="backdrop-blur-xl bg-slate-900/70 border border-slate-700/50 rounded-2xl p-8 shadow-2xl shadow-red-900/20">
            <!-- Decorative Top Border -->
            <div class="absolute top-0 left-8 right-8 h-px bg-gradient-to-r from-transparent via-red-500/50 to-transparent"></div>

            <% if notice %>
              <div class="mb-6 p-4 rounded-lg bg-emerald-500/10 border border-emerald-500/30 text-emerald-400 text-sm font-mono" role="alert">
                <div class="flex items-center space-x-2">
                  <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/></svg>
                  <span><%= notice %></span>
                </div>
              </div>
            <% end %>
            <% if alert %>
              <div class="mb-6 p-4 rounded-lg bg-red-500/10 border border-red-500/30 text-red-400 text-sm font-mono" role="alert">
                <div class="flex items-center space-x-2">
                  <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/></svg>
                  <span><%= alert %></span>
                </div>
              </div>
            <% end %>

            <%= yield %>
          </div>

          <!-- Footer -->
          <div class="text-center mt-8">
            <p class="text-[9px] font-mono text-slate-700">
              MAGI QA v1.0
            </p>
          </div>
        </div>
      </div>
    <% end %>
  </body>
</html>
