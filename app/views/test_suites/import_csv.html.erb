<% content_for :page_title, "IMPORT PROTOCOLS" %>

<% content_for :breadcrumbs do %>
  <%= link_to @test_suite.project.name, project_path(@test_suite.project), class: "text-terminal-cyan hover:text-terminal-cyan/80" %>
  <span class="text-terminal-red mx-2">/</span>
  <%= link_to @test_suite.name, test_suite_path(@test_suite), class: "text-terminal-cyan hover:text-terminal-cyan/80" %>
  <span class="text-terminal-red mx-2">/</span>
  <span class="text-terminal-gray">Import</span>
<% end %>

<div class="max-w-4xl mx-auto space-y-6">
  <% if @errors.present? %>
    <div class="nerv-panel border-terminal-red/50">
      <div class="nerv-panel-header text-terminal-red">Import Errors Detected</div>
      <div class="p-4">
        <ul class="list-disc list-inside text-sm font-mono text-terminal-red max-h-48 overflow-y-auto space-y-1">
          <% @errors.each do |error| %>
            <li><%= error %></li>
          <% end %>
        </ul>
        <% if @imported_count && @imported_count > 0 %>
          <p class="mt-4 text-sm font-mono text-terminal-amber">
            <span class="text-terminal-cyan"><%= @imported_count %></span> protocols were imported successfully despite the errors.
          </p>
        <% end %>
      </div>
    </div>
  <% end %>

  <!-- Upload Form -->
  <div class="nerv-panel">
    <div class="nerv-panel-header flex items-center justify-between">
      <span>IMPORT PROTOCOL DATA</span>
      <span class="text-[10px] font-mono text-terminal-cyan">Bank: <%= @test_suite.name %></span>
    </div>

    <div class="p-6">
      <%= form_with url: process_import_csv_test_suite_path(@test_suite), method: :post, local: true, multipart: true, class: "space-y-6" do |form| %>
        <div>
          <label class="block text-[10px] font-mono text-terminal-amber uppercase tracking-wider mb-2">Select CSV File</label>
          <div class="relative">
            <input type="file" name="csv_file" accept=".csv" required
              class="input-nerv w-full px-4 py-3 text-sm file:mr-4 file:py-2 file:px-4 file:border-0 file:text-sm file:font-mono file:uppercase file:tracking-wider file:bg-terminal-cyan/20 file:text-terminal-cyan hover:file:bg-terminal-cyan/30 file:cursor-pointer">
          </div>
          <p class="mt-2 text-[10px] font-mono text-terminal-gray">Upload CSV file containing protocol data</p>
        </div>

        <div class="flex justify-end space-x-3 pt-6 border-t border-terminal-red/20">
          <%= link_to 'Abort', test_suite_path(@test_suite), class: "px-4 py-2 bg-nerv-purple-mid border border-terminal-gray-dark text-terminal-gray text-xs font-mono uppercase tracking-wider hover:text-terminal-white transition-colors" %>
          <%= form.submit "Import Data", class: "btn-nerv-primary cursor-pointer" %>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Format Documentation -->
  <div class="nerv-panel">
    <div class="nerv-panel-header">CSV FORMAT SPECIFICATION</div>
    <div class="p-6 space-y-6">
      <p class="text-sm font-mono text-terminal-white/80">
        Download the template or use the following format for your CSV file:
      </p>

      <div>
        <%= link_to csv_template_test_suite_path(@test_suite), class: "btn-nerv-primary inline-flex items-center space-x-2" do %>
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
          </svg>
          <span>Download Template</span>
        <% end %>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <h3 class="text-[10px] font-mono text-terminal-amber uppercase tracking-widest mb-3">Required Columns</h3>
          <ul class="space-y-2 text-sm font-mono text-terminal-white/80">
            <li><span class="text-terminal-cyan">scope_path</span> - Folder hierarchy (e.g., "Login" or "Login/Validation")</li>
            <li><span class="text-terminal-cyan">title</span> - Protocol designation</li>
            <li><span class="text-terminal-cyan">steps</span> - Execution steps (use \n for new lines)</li>
            <li><span class="text-terminal-cyan">expected_result</span> - Expected outcome (use \n for new lines)</li>
          </ul>
        </div>

        <div>
          <h3 class="text-[10px] font-mono text-terminal-amber uppercase tracking-widest mb-3">Optional Columns</h3>
          <ul class="space-y-2 text-sm font-mono text-terminal-white/80">
            <li><span class="text-terminal-cyan">preconditions</span> - Initial conditions</li>
            <li><span class="text-terminal-cyan">cypress_id</span> - Automation ID (e.g., TC-001)</li>
          </ul>
        </div>
      </div>

      <div>
        <h3 class="text-[10px] font-mono text-terminal-amber uppercase tracking-widest mb-3">Example Data</h3>
        <div class="bg-nerv-black/50 border border-terminal-cyan/30 p-4 overflow-x-auto">
          <pre class="text-xs font-mono text-terminal-cyan">scope_path,title,preconditions,steps,expected_result,cypress_id
Login,Valid login test,User exists,1. Enter email\n2. Enter password\n3. Click Login,User is redirected to dashboard,TC-001
Login/Validation,Empty email test,,1. Leave email empty\n2. Click Login,Error message is shown,TC-002</pre>
        </div>
      </div>

      <div class="p-4 bg-terminal-amber/10 border border-terminal-amber/30">
        <div class="flex items-center space-x-2">
          <svg class="w-5 h-5 text-terminal-amber" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
          <span class="text-sm font-mono text-terminal-amber">System Note:</span>
        </div>
        <p class="mt-2 text-sm font-mono text-terminal-white/80">
          Test scopes (folders) will be created automatically if they don't exist.
        </p>
      </div>
    </div>
  </div>
</div>
