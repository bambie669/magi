<h1 class="text-2xl font-bold mb-6">Editing Test Suite: <%= @test_suite.name %></h1>

<%= render 'form', test_suite: @test_suite %>

<div class="mt-4">
  <%= link_to 'Show Suite', @test_suite, class: "text-blue-600 hover:underline mr-4" %> |
  <%= link_to 'Back to Project', project_path(@test_suite.project), class: "text-blue-600 hover:underline ml-4" %>
</div>